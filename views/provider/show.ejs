<%- layout('/layouts/boilerplate') %>

<body>
   <div class="new-container" style="margin-top: 60px; background-color:rgb(246, 230, 219)">
     <div class="row mt-4">
      <div class="col-8 offset-3">
           <h1><b> <%= menu.title %></b></h1><br>
      </div>     
      <div class="card col-5 offset-3 show-card" >
            <img src="<%= menu.image.url %>" class="card-img-top show-img" alt="menu_image" />
      </div><br>
      <div class="card-body offset-3">
         <h3>Menu Details</h3>
         <p class="card-text">
             <br/> 
               Posted by: <b><%= menu.owner.username || "UnKnown user" %></b> <br/> 
            <%= menu.description %> <br/>
             &#8377; <%= menu.price.toLocaleString('en-IN') %> <small class="text-muted">+18% GST</small>/plate <br/>
          
         </p><br>
      </div>
      <% if(currUser && menu.owner._id.equals(currUser._id)) { %>
       <div class="btns offset-3 ">
            <a href="/provider/menu/<%=menu._id%>/edit" id="show-btn" class="btn btn-primary col-1 offset-2  ">Update </a><br><br>
         <form method="POST"  action="/provider/menu/<%=menu._id%>?_method=DELETE">
            <button id="show-btn" class="btn btn-secondary offset-2 ">Delete </button>
         </form>
       </div>         
        <%}%>
      </div> 
       <% if(currUser && currUser.role ==="customer") { %>
     <div class="col-8 offset-3 mb-4">
        <a href="/provider/menu/<%= menu._id %>/order" class="btn btn-primary">Order Now</a>
         <a  id="show-btn" class="btn btn-secondary offset-2 " href="/user/index">CANCEL</a>
   </div>
    <%}%>        
   <div class="col-8 offset-3">
      <% if(currUser){%>
      <hr>
         <h4>Leave a Review</h4>
         <form action="/provider/menu/<%= menu._id %>" method="POST" novalidate class="needs-validation">
            <div>
            <fieldset class="starability-slot">
               <legend>rating:</legend>
               <input type="radio" id="no-rate" class="input-no-rate" name="review[rating]" value="1" checked aria-label="No rating." />
                <input type="radio" id="first-rate1" name="review[rating]" value="1" />
                <label for="first-rate1" title="Terrible">1 star</label>
                <input type="radio" id="first-rate2" name="review[rating]" value="2" />
                <label for="first-rate2" title="Not good">2 stars</label>
               <input type="radio" id="first-rate3" name="review[rating]" value="3" />
                <label for="first-rate3" title="Average">3 stars</label>
               <input type="radio" id="first-rate4" name="review[rating]" value="4" />
                <label for="first-rate4" title="Very good">4 stars</label>
               <input type="radio" id="first-rate5" name="review[rating]" value="5" />
               <label for="first-rate5" title="Amazing">5 stars</label>
            </fieldset>
            </div>
            <br>
            <div>
               <label for="comment" class="form-label">Comments</label>
               <textarea name="review[comment]" id="comment" cols="20" rows="5" class=" form-control" required></textarea>
                <div class="invalid-feedback">
               Please add some comment for Review.
                 </div>
            </div>
            <br>
            <button class="btn btn-primary">Submit Review</button>
         </form>
         <hr>
         <%}%>
         <% if(menu.reviews.length > 0) { %>
         <h4><b>All Reviews<b></h4>
            <div class="row">
               <% for(review of menu.reviews) { %>
              <div class="card col-5 ms-3 md-3">
               <div class="card-body">
                  <h5>Posted:<%= review.author ? review.author.username:"unknown user"%></h5>
                   <p class="starability-result" data-rating="<%= review.rating %>">Rated: <%= review.rating %> stars</p>
                   <p><%= review.comment %> </p> 
                  <form class="mb-3" method="POST" action="/provider/menu/<%= menu._id %>/reviews/<%= review._id %>?_method=DELETE">
                     <button class="btn btn-danger btn-sm">Delete Review</button>
                  </form>
               </div>
               </div>
             <% } %>
            </div>
         <%}%>
    <div id="map"></div>
   </div>
</body>